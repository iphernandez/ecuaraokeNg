import { SongFilterPipe } from './song-filter.pipe';

describe('SongFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SongFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
